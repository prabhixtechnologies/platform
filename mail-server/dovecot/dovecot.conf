# Dovecot master config — IMAP store + LMTP delivery + SASL for Postfix submission.

protocols = imap lmtp
listen = *, ::
!include conf.d/*.conf

# SQL driver for future password_hash column; bootstrap uses passwd-file (see 10-auth.conf).
!include dovecot-sql.conf.ext
