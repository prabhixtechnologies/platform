package com.prabhix.platform.observability.taxonomy;

/**
 * Severity for both structured application logs and persisted event rows.
 */
public enum LogSeverity {
    DEBUG,
    INFO,
    WARN,
    ERROR,
    FATAL
}
